# start_server.sh 가 이 저장소를 클론한 뒤 이 파일로 uv pip install 을 돌립니다.
# 파일이 없으면 "Failed to install Python requirements" 로 워커가 죽습니다.
#
# 이미지에 굽는 것이 아니라 런타임에 설치되므로, 여기를 고치면 재빌드 없이
# 새로 뜨는 워커부터 반영됩니다. 뒤집으면, 깨진 채로 main 에 밀면
# 새 워커가 전부 죽습니다 (11차 6장).

vastai-sdk>=0.3.0
aiohttp>=3.9
